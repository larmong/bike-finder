import styled from "@emotion/styled";

export const Timer = styled.div`
  position: absolute;
  right: -50px;
  z-index: 1;
  width: 40px;
  text-align: center;
  font-weight: 700;
  font-size: 14px;
  letter-spacing: 0.1em;
  color: #0d8f68;
`;
